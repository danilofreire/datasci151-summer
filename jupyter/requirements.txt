# Host-side build dependencies for JupyterLite.
#
# These are NOT the packages students use in their browser. The
# Pyodide kernel ships its own pandas, numpy, matplotlib, scipy,
# scikit-learn, sympy, etc. via the Pyodide CDN. To pre-load extra
# packages inside a notebook, use:
#
#   import piplite
#   await piplite.install("polars")

jupyterlite-core==0.5.0
jupyterlite-pyodide-kernel==0.5.0
jupyterlab~=4.3.4
notebook~=7.3.2
ipywidgets>=8.1.3,<9
